---
---

Wire the owner-phone approval demo end to end: EXPERIMENTAL PREVIEW relay
routes (`GET /native/projections/{operationId}`,
`POST /native/decisions/{operationId}`) with a versioned consent projection,
the widened Swift decoders and consent screen, a runnable iOS demo app with
pairing, and `examples/phone`. Preview surfaces only — no release.
