---
---

Add the @oaath/sdk browser client composition: `createOAAth`, connect,
requestPermission, resume, signOut, close, the app-facing Grant and Operation
handles, one current IndexedDB schema with in-transaction compare-and-swap and
non-extractable key custody, in-memory backends, and the cleanup coordinator with
durable effect checkpoints. No release.
