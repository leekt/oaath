---
---

Compose consumer-authored @oaath/sdk Kernel key profiles: one bounded
`custom:<slug>` kind, its caller-bound validator and permission signer module, and
mandatory signature self-verification at the capture boundary, through the same
owner and session operators and the one composition factory. No release.
