---
---

Add the @oaath/server durable authorization relay: Fetch handler, row-locked
one-time authorization code and encrypted artifact transitions, and the
PostgreSQL store behind the `./postgres` subpath. No release.
