---
---

Migrate the experimental owner-phone iOS app into `native/ios`: a plain SPM
SwiftUI package that decodes the closed OAAth APNs payload, renders the opaque
owner-phone projection exactly as the relay sends it, and submits approve or
reject with honest replayed-outcome semantics. Preview only. Not in the release
group; never published to npm. No release.
