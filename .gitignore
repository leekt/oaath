node_modules/
dist/
coverage/
.artifacts/
*.tgz
*.log
.DS_Store

# Agent scratch state (worktrees, session files) — never commit
.claude/

# Environment files carry credentials — never commit any of them
.env
.env.*
