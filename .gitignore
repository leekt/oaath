node_modules/
dist/
coverage/
.artifacts/
*.tgz
*.log
.DS_Store

# Local env files (credentials, e.g. examples/.env) — never commit
.env
.env.*

# Agent scratch state (worktrees, session files) — never commit
.claude/
